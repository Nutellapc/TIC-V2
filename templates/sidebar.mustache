<div class="flex">
    <!-- Botón para abrir el sidebar en pantallas pequeñas -->
    <button
        class="fixed top-4 left-4 z-50 p-2 bg-gray-900 text-white rounded-lg lg:hidden"
        onclick="document.querySelector('aside').classList.toggle('hidden');"
    >
        <span class="material-icons">menu</span>
    </button>

    <!-- Sidebar -->
    <aside
        class="fixed left-0 top-0 z-50 h-full w-64 bg-gray-900 text-white shadow-lg hidden lg:block"
    >
        <!-- SIDEBAR HEADER -->
        <div class="flex items-center justify-between gap-2 p-6 border-b border-gray-700">
            <a href="#">
                <img src="./images/logo/logo.svg" alt="Logo" class="h-8 w-auto" />
            </a>
        </div>

        <!-- SIDEBAR NAVIGATION -->
        <nav class="mt-4 flex flex-col space-y-2">
            <a href="index.php" class="flex items-center py-2 px-6 hover:bg-gray-700">
                <span class="material-icons">dashboard</span>
                <span class="ml-3">Dashboard</span>
            </a>
            <a href="profiles.php" class="flex items-center py-2 px-6 hover:bg-gray-700">
                <span class="material-icons">person</span>
                <span class="ml-3">Profiles</span>
            </a>
            <a href="settings.php" class="flex items-center py-2 px-6 hover:bg-gray-700">
                <span class="material-icons">settings</span>
                <span class="ml-3">Settings</span>
            </a>
            <a href="logout.php" class="flex items-center py-2 px-6 hover:bg-gray-700">
                <span class="material-icons">logout</span>
                <span class="ml-3">Logout</span>
            </a>
        </nav>
    </aside>


</div>
